# 提案用モックサイトは検索エンジンにインデックスさせない
# （配下の全ファイルに適用。WordPress側のモックにも効くようHTTPヘッダーで指定）
<IfModule mod_headers.c>
  # always指定: PHP(CGI)応答にもヘッダーを付与するため必須
  Header always set X-Robots-Tag "noindex, nofollow"
</IfModule>
